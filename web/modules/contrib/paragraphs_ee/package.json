{
  "name": "paragraphs_ee",
  "version": "1.0.0",
  "description": "Paragraphs Editor Enhancements for better usability.",
  "author": "stBorchert",
  "private": true,
  "engines": {
    "yarn": ">= 1.6",
    "node": ">=18.0.0"
  },
  "scripts": {
    "compile": "node-sass --recursive --output=css --output-style=compressed scss",
    "watch": "yarn compile --watch"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
